# Automatic trust and import in authorized keys

To automatically import the keys into authorized_keys, run:

```bash

curl https://raw.githubusercontent.com/johnverbiest/crypto-identity/refs/heads/master/scripts/update-authorized-keys.sh | bash

```